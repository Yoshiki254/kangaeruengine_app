
<% modal ||= false %>
<% remote = modal ? true : false %>
<div class="modal fade" id="new_users_interview_modal" >
	<div class="modal-dialog">
    <div class="modal-header">
    	<button type="button" class="close" id="modal-close" data-dismiss="modal" aria-hidden="true">&times;</button>
    	<p>10ケースマラソン</p>
        <h4 class="modal-title" id="myModalLabel"><%= interview.name %></h4>
    </div>
		<div class="modal-body">
			<%= form_for [current_user, @users_interview], remote: remote, html: {role: :form, 'data-model' => 'users_interview'} do |f| %>

			  <% if @users_interview.errors.any? %>
			    <div id="error_explanation">
			      <ul>
				      <% @users_interview.errors.full_messages.each do |msg| %>
				        <li><%= msg %></li>
				      <% end %>
			      </ul>
			    </div>
			  <% end %>

			  <div class="form-group">
			    <%= f.label :実施日, class: 'control-label' %>
			    <strong><%= f.date_field :date, class: 'form-control' %></strong>
			    <span class="help-block"></span>
			    <%= f.hidden_field :interview_id, :value => interview.id %>
			  </div>

			  <div class="actions">
			    <%= f.submit 'Submit', id: "btn btn-submit" %>
			  </div>
			<% end %>
    </div>
	</div>
</div>

