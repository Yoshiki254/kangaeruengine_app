
$(document).on('ready turbolinks:load', function(){
  $( '#slider1' ).sliderPro({

  	width: 900, 
	  height: 600,
    responsive: true,  
    arrows: true,
    autoplay: false,
    forceSize: 'fullwidth',
  });
});